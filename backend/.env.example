DATABASE_URL=postgres://leah:replace-me@localhost:5432/leah?sslmode=disable
JWT_SECRET=replace-with-at-least-32-random-characters
PORT=8080
HOST=127.0.0.1
CORS_ORIGINS=http://localhost:5173

# Used only by cmd/bootstrap-root. Choose a private email, run the bootstrap,
# then remove ROOT_PASSWORD from the local .env file.
DATABASE_OWNER_URL=postgresql:///leah
ROOT_EMAIL=choose-a-private-root-email@example.com
ROOT_PASSWORD=replace-with-a-unique-password-of-at-least-16-characters
